
[{{ range $index, $page := .Site.Pages }}
  {{ $html_h := findRE "</h2>(.|\n)*?<h2" $page.Content}},
  {{- if ne $page.Type "json" -}}
    {{- if and $index (gt $index 0) -}},
    {{- end }}
  {{- end -}}
{{- end -}}]

